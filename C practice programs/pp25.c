#include<stdio.h>
#define pi 3.14

int main(){

    float r;
    printf("Enter the radius of the circle");
    scanf("%f",&r);
    printf("The perimeter of the cirle is %f",(2*pi*r));
    return 0;
}