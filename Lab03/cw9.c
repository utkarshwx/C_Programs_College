//WAP to calculate the area of a circle.

#include<stdio.h>

int main(){

    float r;
    printf("Enter the radius of the circle");
    scanf("%f",&r);
    printf("area = %.4f",(3.14*r*r));
    return 0;
}